{
  "name": "node-rpicam-server",
  "version": "0.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server",
    "prestart": "cd ../web && npm run build",
    "start:watch": "parallelshell 'NODE_ENV=dev node server' 'cd ../web && npm run dev'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ejs": "^2.2.1",
    "express": "^4.11.0",
    "socket.io": "^1.2.1",
    "socket.io-client": "^1.2.1"
  },
  "devDependencies": {
    "parallelshell": "^1.0.3"
  }
}
